// @generated by "php idl.php inc {input.idl.php} {output.inc}"

#if EXT_TYPE == 0
"cached_json_decode", T(Variant), S(0), "json_file", T(String), NULL, NULL, S(0), "assoc", T(Boolean), "b:0;", "false", S(0), "loose", T(Boolean), "b:0;", "false", S(0), NULL, S(16384), "/**\n * ( excerpt from http://php.net/manual/en/function.cached-json-decode.php\n * )\n *\n * Wrapper over json_decode to add caching. Works the same way as\n * json_decode but for the first argument, which has to but a file path so\n * we could get the modification time and check for stale data on the\n *     cache\n *\n * @json_file  string  The json file being decoded.\n *\n *                     This function only works with UTF-8 encoded data.\n * @assoc      bool    When TRUE, returned objects will be converted into\n *                     associative arrays.\n * @loose      bool    User specified recursion depth.\n *\n * @return     mixed   Returns an array containing the decoded json, time\n *                     of last modification of the file, and last time the\n *                     file modification time was checked\n */", 

#elif EXT_TYPE == 1

#elif EXT_TYPE == 2

#elif EXT_TYPE == 3

#endif
