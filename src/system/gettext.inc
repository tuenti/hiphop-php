// @generated by "php idl.php inc {input.idl.php} {output.inc}"

#if EXT_TYPE == 0
"textdomain", T(Variant), S(0), "domain", T(String), NULL, NULL, S(0), NULL, S(16384), "/**\n * ( excerpt from http://php.net/manual/en/function.textdomain.php )\n *\n * Change the textdomain.\n *\n * @domain     string  Textdomain to set.\n *\n * @return     mixed   Returns the current domain or false on error.\n */", 
"gettext", T(Variant), S(0), "msgid", T(String), NULL, NULL, S(0), NULL, S(16384), "/**\n * ( excerpt from http://php.net/manual/en/function.gettext.php )\n *\n * Return the translation of msgid for the current domain, or\n * \tmsgid unaltered if a translation does not exist.\n *\n * @msgid      string  String to translate.\n *\n * @return     mixed   Returns the msgid translated or false on error.\n */", 
"dgettext", T(Variant), S(0), "domain_name", T(String), NULL, NULL, S(0), "msgid", T(String), NULL, NULL, S(0), NULL, S(16384), "/**\n * ( excerpt from http://php.net/manual/en/function.dgettext.php )\n *\n * Return the translation of msgid for domain_name, or msgid\n * \tunaltered if a translation does not exist.\n *\n * @domain_name\n *             string  Domain of the translation.\n * @msgid      string  String to translate.\n *\n * @return     mixed   Returns the msgid translated of false on error.\n */", 
"dcgettext", T(Variant), S(0), "domain_name", T(String), NULL, NULL, S(0), "msgid", T(String), NULL, NULL, S(0), "category", T(Int64), NULL, NULL, S(0), NULL, S(16384), "/**\n * ( excerpt from http://php.net/manual/en/function.dcgettext.php )\n *\n * Return the translation of msgid for domain_name and category,\n * \tor msgid unaltered if a translation does not exist.\n *\n * @domain_name\n *             string  Domain of the translation.\n * @msgid      string  String to translate.\n * @category   int     Category of the translation.\n *\n * @return     mixed   Returns the msgid translated or false on error.\n */", 
"bindtextdomain", T(Variant), S(0), "domain_name", T(String), NULL, NULL, S(0), "dir", T(String), NULL, NULL, S(0), NULL, S(16384), "/**\n * ( excerpt from http://php.net/manual/en/function.bindtextdomain.php )\n *\n * Bind to the text domain domain_name, looking for translations\n * \tin dir. Returns the current domain.\n *\n * @domain_name\n *             string  Domain of the translation.\n * @dir        string  Directory to look for translations.\n *\n * @return     mixed   Returns the current domain on success or false on\n *                     error.\n */", 
"ngettext", T(Variant), S(0), "msg", T(String), NULL, NULL, S(0), "msg_plural", T(String), NULL, NULL, S(0), "n", T(Int64), NULL, NULL, S(0), NULL, S(16384), "/**\n * ( excerpt from http://php.net/manual/en/function.ngettext.php )\n *\n * Plural version of gettext\n *\n * @msg        string  Message to translate.\n * @msg_plural string  Plural message to translate.\n * @n          int     Plural chooser.\n *\n * @return     mixed   Returns the appropriate translated plural of the msg\n *                     or false\n *                     \t  on error.\n */", 
"dngettext", T(Variant), S(0), "domain", T(String), NULL, NULL, S(0), "msg", T(String), NULL, NULL, S(0), "msg_plural", T(String), NULL, NULL, S(0), "n", T(Int64), NULL, NULL, S(0), NULL, S(16384), "/**\n * ( excerpt from http://php.net/manual/en/function.dngettext.php )\n *\n * Plural version of dgettext\n *\n * @domain     string  Domain of the translation.\n * @msg        string  Message to translate.\n * @msg_plural string  Plural message to translate.\n * @n          int     Plural chooser.\n *\n * @return     mixed   Returns the appropriate translated plural of the msg\n *                     or false\n *                     \t  on error.\n */", 
"dcngettext", T(Variant), S(0), "domain", T(String), NULL, NULL, S(0), "msg", T(String), NULL, NULL, S(0), "msg_plural", T(String), NULL, NULL, S(0), "n", T(Int64), NULL, NULL, S(0), "category", T(Int64), NULL, NULL, S(0), NULL, S(16384), "/**\n * ( excerpt from http://php.net/manual/en/function.dcngettext.php )\n *\n * Plural version of dcgettext\n *\n * @domain     string  Domain of the translation.\n * @msg        string  Message to translate.\n * @msg_plural string  Plural message to translate.\n * @n          int     Plural chooser.\n * @category   int     Category of the translation.\n *\n * @return     mixed   Returns the appropriate translated plural of the msg\n *                     or false\n *                     \t  on error.\n */", 
"bind_textdomain_codeset", T(Variant), S(0), "domain", T(String), NULL, NULL, S(0), "codeset", T(String), NULL, NULL, S(0), NULL, S(16384), "/**\n * ( excerpt from\n * http://php.net/manual/en/function.bind-textdomain-codeset.php )\n *\n * Specify the character encoding in which the messages from the\n * \tDOMAIN message catalog will be returned.\n *\n * @domain     string  Target domain\n * @codeset    string  Character encoding to set.\n *\n * @return     mixed   Returns string on success and false otherwise.\n */", 

#elif EXT_TYPE == 1

#elif EXT_TYPE == 2

#elif EXT_TYPE == 3

#endif
