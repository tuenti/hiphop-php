// @generated by "php idl.php inc {input.idl.php} {output.inc}"

#if EXT_TYPE == 0
"get_bucket_cache_info", T(Variant), S(0), "cluster_id", T(Int64), NULL, NULL, S(0), "bucket_id", T(Int64), NULL, NULL, S(0), NULL, S(16384), "/**\n * ( excerpt from\n * http://php.net/manual/en/function.get-bucket-cache-info.php )\n *\n *\n * @cluster_id int\n * @bucket_id  int\n *\n * @return     mixed\n */", 
"set_bucket_cache_info", T(Boolean), S(0), "cluster_id", T(Int64), NULL, NULL, S(0), "bucket_id", T(Int64), NULL, NULL, S(0), "state", T(Int64), NULL, NULL, S(0), "server", T(Int64), NULL, NULL, S(0), "volume", T(Int64), NULL, NULL, S(0), "alt_server", T(Int64), NULL, NULL, S(0), "alt_volume", T(Int64), NULL, NULL, S(0), NULL, S(16384), "/**\n * ( excerpt from\n * http://php.net/manual/en/function.set-bucket-cache-info.php )\n *\n *\n * @cluster_id int\n * @bucket_id  int\n * @state      int\n * @server     int\n * @volume     int\n * @alt_server int\n * @alt_volume int\n *\n * @return     bool\n */", 

#elif EXT_TYPE == 1
"BUCKET_INFO_CLUSTER", T(Int64),
"BUCKET_INFO_ID", T(Int64),
"BUCKET_INFO_STATE", T(Int64),
"BUCKET_INFO_SERVER", T(Int64),
"BUCKET_INFO_VOLUME", T(Int64),
"BUCKET_INFO_ALT_SERVER", T(Int64),
"BUCKET_INFO_ALT_VOLUME", T(Int64),

#elif EXT_TYPE == 2

#elif EXT_TYPE == 3

#endif
